#include <stdio.h>
#include <stdlib.h>

int main(){

  double num = sizeof(double);
  int vet[10];

  printf("A quantidade do vetor eh: %d\n",sizeof(vet));
  printf("O numero eh: %d\n",sizeof(num));

  return 0;
}